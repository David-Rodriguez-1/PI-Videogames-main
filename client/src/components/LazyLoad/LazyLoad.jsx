import style from './LazyLoad.module.css'

export const Lazy = () =>{
  return (
    <div className={style.card_lazy}>
      {/* <h3>Cargando...</h3> */}
      {/* <img src={game.background_image} alt={game.name} />
      <h3>{game.name}</h3>
      <p className={style.rating}>⭐{game.rating}</p>
      <p>{game.genres.map((g) => g.name)}</p> */}
    </div>
  )
}

// export default Lazy
